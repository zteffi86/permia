# Permía Backend

FastAPI backend for deterministic permit evaluation.

## Setup

```bash
python -m venv venv
source venv/bin/activate
pip install -e .
```

## Run

```bash
uvicorn src.main:app --reload
```

## Test

```bash
pytest
```
